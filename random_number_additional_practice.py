import random
# random_numbers = set(random.randint(1,39))
# for number in random_numbers:
#     random_numbers.add(number)
# print(lotto_numbers)
random_numbers = set()
print(type(random_numbers))

while len(random_numbers) < 11=:
    number = random.randint(1,69)
    random_numbers.add(number)
print(random_numbers)

random_numbers_sorted = sorted(random_numbers)
print("Sorted random number are: ", random_numbers_sorted)